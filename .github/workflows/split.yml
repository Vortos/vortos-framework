name: Monorepo Split

on:
  push:
    branches:
      - main
    tags:
      - '*'

jobs:
  split:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package:
          - local_path: 'packages/Vortos/src/Domain'
            split_repository: 'vortos/vortos-domain'
          - local_path: 'packages/Vortos/src/Foundation'
            split_repository: 'vortos/vortos-foundation'
          - local_path: 'packages/Vortos/src/Http'
            split_repository: 'vortos/vortos-http'
          - local_path: 'packages/Vortos/src/Logger'
            split_repository: 'vortos/vortos-logger'
          - local_path: 'packages/Vortos/src/Cache'
            split_repository: 'vortos/vortos-cache'
          - local_path: 'packages/Vortos/src/Tracing'
            split_repository: 'vortos/vortos-tracing'
          - local_path: 'packages/Vortos/src/Persistence'
            split_repository: 'vortos/vortos-persistence'
          - local_path: 'packages/Vortos/src/PersistenceDbal'
            split_repository: 'vortos/vortos-persistence-dbal'
          - local_path: 'packages/Vortos/src/PersistenceMongo'
            split_repository: 'vortos/vortos-persistence-mongo'
          - local_path: 'packages/Vortos/src/Messaging'
            split_repository: 'vortos/vortos-messaging'
          - local_path: 'packages/Vortos/src/Cqrs'
            split_repository: 'vortos/vortos-cqrs'
          - local_path: 'packages/Vortos/src/Auth'
            split_repository: 'vortos/vortos-auth'
          - local_path: 'packages/Vortos/src/Authorization'
            split_repository: 'vortos/vortos-authorization'

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: symplify/monorepo-split-github-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.MONOREPO_SPLIT_TOKEN }}
        with:
          package_directory: ${{ matrix.package.local_path }}
          repository_organization: 'vortos'
          repository_name: ${{ matrix.package.split_repository }}
          user_name: 'vortos-bot'
          user_email: 'bot@vortos.dev'
